# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SwiftWorkShop' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire', '~> 4.7.3'
  pod 'AlamofireImage'
  pod 'ObjectMapper', '~> 3.3'

  # Pods for SwiftWorkShop

  target 'SwiftWorkShopTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SwiftWorkShopUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
